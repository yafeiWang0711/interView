### key的作用
    1. 唯一标识：key 是 Vue 中用于唯一标识组件实例的属性。当 Vue 进行虚拟 DOM  diff 算法时，会根据 key 来判断两个节点是否是同一个节点，从而高效地更新 DOM。
    2. 列表渲染：在使用 v-for 指令渲染列表时，每个列表项都需要一个唯一的 key。这是因为 Vue 会根据 key 来跟踪列表项的变化，从而实现高效的列表更新。
    3. 状态管理：key 还可以用于管理组件的状态。当组件被销毁时，Vue 会根据 key 来判断是否需要保留组件的状态。